# secrets & machine-local config
.env
config.yaml

# pipeline outputs / intermediates
work/

# input videos / finished dubs: keep the folders in git, ignore their (large) contents
input/*
!input/.gitkeep
output/*
!output/.gitkeep

# python
__pycache__/
*.pyc
.venv/
venv/
