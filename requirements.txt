# Core pipeline dependencies.
# ffmpeg + demucs CLIs must ALSO be installed and on PATH (see README).

# Transcription + word-level timestamps + speaker diarization
whisperx>=3.1.1

# Source separation (dialogue vs music/SFX). Installs the `demucs` CLI too.
demucs>=4.0.1

# Georgian TTS
elevenlabs>=1.0.0

# Translation (nl -> ka) via Claude
anthropic>=0.40.0

# Audio assembly / overlay / gain
pydub>=0.25.1
# pydub imports stdlib `audioop`, removed in Python 3.13 — this restores it.
audioop-lts; python_version >= "3.13"

# Config + env + progress
pyyaml>=6.0
python-dotenv>=1.0.0
tqdm>=4.66.0
