# Copy this file to `.env` and fill in the key. Both the CLI (autodub.py)
# and the GUI (python -m gui) load it automatically.
# `.env` is gitignored — never commit real keys (not here either!).

# ElevenLabs Dubbing API — billed per minute of source audio
# (~$0.33/min watermarked, ~$0.50/min clean).
# Get it: https://elevenlabs.io -> Profile -> API Keys
ELEVENLABS_API_KEY=
